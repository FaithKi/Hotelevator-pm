package utils;

import logic.game.PatienceLevel;

public class CustomerUtils {
	
	public static void patienceLevel(PatienceLevel patienceLevel) {
		switch (patienceLevel) {
		case value: {
			
			yield type;
		}
		default:
			throw new IllegalArgumentException("Unexpected value: " + patienceLevel);
		}
		
	}
	public double deviator() {
		
	}
}
